# Agents

This file is a stub for AI coding agents that look for `AGENTS.md` rather than `CLAUDE.md`. The canonical agent doc is **[CLAUDE.md](./CLAUDE.md)** — open that.

(Stub used instead of a symlink for cross-platform compatibility — symlinked Markdown files render as plain text on stock Git for Windows.)
