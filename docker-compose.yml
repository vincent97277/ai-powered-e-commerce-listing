# Local-first hackathon stack
# Start: docker compose up -d
# Stop: docker compose down
# Reset: docker compose down -v  (drop volume, wipe all data)

services:
  postgres:
    image: postgres:16-alpine
    container_name: rls-ai-shop-postgres
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: rls_ai_shop
      POSTGRES_USER: owner
      POSTGRES_PASSWORD: owner_pass
      # Enable plpgsql + required extensions
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - pg_data:/var/lib/postgresql/data
      - ./db/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U owner -d rls_ai_shop"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pg_data:
    name: rls-ai-shop-pg-data
